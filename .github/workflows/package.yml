name: Package
on: [push, pull_request]

jobs:
  package-macos:
    runs-on: macos-latest
    env:
      PIPENV_VENV_IN_PROJECT: 1
      PIPENV_IGNORE_VIRTUALENVS: 1
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python Environment
        run: |
          pip3 install pipenv
          pipenv install pyqt6 lxml
          pipenv run pip install pyqt6 lxml
      - name: Build LabelImg
        run: |
          # pyrcc is handled by the app initialization in PyQt6 if needed
          rm -rf build dist
      - name: Package LabelImg
        run: |
          pipenv run python setup.py py2app
          open dist/labelImg.app
      - name: Archive macOS app
        run: |
          cd dist/
          tar czf labelImg.tgz labelImg.app
      - uses: actions/upload-artifact@v3
        with:
          name: macOS artifact
          path: dist/labelImg.tgz
  package-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python Environment
        run: |
          pip3 install pyinstaller pyqt6 lxml
      - name: Build LabelImg
        run: |
          # Resource compilation is handled within the app
          echo "Building..."
      - name: Package LabelImg
        run: |
          pyinstaller --hidden-import=PyQt6 --hidden-import=lxml -F -n "labelImg" -c labelImg.py -p ./libs -p ./
      - uses: actions/upload-artifact@v3
        with:
          name: Windows artifact
          path: dist/labelImg.exe
  package-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python Environment
        run: |
          pip3 install pyinstaller pyqt6 lxml
      - name: Build LabelImg
        run: |
          # Resource compilation is handled within the app
          echo "Building..."
      - name: Package LabelImg
        run: |
          pyinstaller --hidden-import=PyQt6 --hidden-import=lxml -F -n "labelImg" -c labelImg.py -p ./libs -p ./
      - uses: actions/upload-artifact@v3
        with:
          name: Linux artifact
          path: dist/labelImg

